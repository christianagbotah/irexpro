"""
Backtest placeholder — offline research only.

No profit or performance claims. Real backtesting deferred to future sprints.
"""


def run_backtest_placeholder(dataset_path: str) -> dict:
    return {
        "status": "not_implemented",
        "message": "Backtest scaffold only — no performance metrics available",
        "dataset_path": dataset_path,
    }
