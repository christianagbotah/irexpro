import { ConfigService } from '@nestjs/config';
import { BasePaymentProvider } from './base-provider';
import { PaystackHttpClient } from './paystack-http.client';
import { CreateCheckoutSessionRequest, CreateCheckoutSessionResult, PaymentProviderTransactionStatus, ProviderWebhookEvent } from '../interfaces/payment-provider.interface';
export declare class PaystackPaymentProvider extends BasePaymentProvider {
    private readonly configService;
    private readonly httpClient;
    private readonly logger;
    readonly providerId = "paystack";
    readonly displayName = "Paystack";
    readonly supportedCountries: string[];
    readonly supportedCurrencies: string[];
    readonly supportedPaymentMethods: string[];
    readonly isLive: boolean;
    constructor(configService: ConfigService, httpClient: PaystackHttpClient);
    private isEnabled;
    private getSecretKey;
    private getWebhookSecret;
    private getBaseUrl;
    private getCallbackUrl;
    private assertConfigured;
    createCheckoutSession(request: CreateCheckoutSessionRequest): Promise<CreateCheckoutSessionResult>;
    private buildOutboundMetadata;
    verifyWebhookSignature(rawBody: Buffer, headers: Record<string, string | string[] | undefined>): boolean;
    parseWebhookEvent(rawBody: Buffer, _headers: Record<string, string | string[] | undefined>): ProviderWebhookEvent;
    private buildProviderEventId;
    private safeInboundMetadata;
    getTransactionStatus(providerReference: string): Promise<PaymentProviderTransactionStatus>;
    private mapTransactionStatus;
    private safeMessage;
}
