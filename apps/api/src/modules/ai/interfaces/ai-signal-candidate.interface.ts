/**
 * Re-export from StrategyModule for use by AiModule.
 * AiSignalCandidate is the shared contract between the AI Signal Engine
 * and the Strategy Orchestrator.
 */
export type { AiSignalCandidate } from '../../strategy/interfaces/strategy.interface';
